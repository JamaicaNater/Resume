export default class Resume {
    constructor(education, projects, user, references, experience) {
        this.education = education;
        this.projects = projects;
        this.user = user;
        this.references = references;
        this.experience = experience;
    }
}
