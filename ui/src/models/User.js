export default class User {
    constructor(firstName, lastName, phoneNumber, email, skills, summary) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.phoneNumber = phoneNumber;
        this.email = email;
        this.skills = skills;
        this.summary = summary;
    }
}
